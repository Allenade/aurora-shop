# Session cookie signing (required in production)
SESSION_SECRET=aurora-dev-session-secret-change-me

# mock = local BFF mocks (default) | upstream = call BACKEND_URL
AUTH_MODE=mock

# Real API base URL when AUTH_MODE=upstream (no trailing slash)
# BACKEND_URL=http://localhost:4000
